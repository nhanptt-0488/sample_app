<%= form_for @micropost do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t(".compose") %>
  </div>
  <span class="image">
    <%= f.file_field :image, accept: Settings.image_types.join(", ") %>
  </span>
  <%= f.submit t(".post"), class: "btn btn-primary" %>
<% end %>
